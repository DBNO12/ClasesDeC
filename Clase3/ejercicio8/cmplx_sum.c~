#include <stdio.h>
#include "complx.h"

struct cmplx cmplx_sum(const struct cmplx *a, const struct cmplx *b)
{
  struct complx c;

  c.real = *(a.real) + *(b.real);
  c.img  = *(a.img)  + *(b.img);

  return c;
}
