#include <stdio.h>

struct 64bits
{
  char c1[4];
  char c2[4];
  char c3[4];
  char c4[4];
};

int main()
{
  struct 64bits palabro = {{a,a,a,a},{b,b,b,b},{c,c,c,c},{d,d,d,d}};
  
  printf("%d", palabro};

  return 0;
}
